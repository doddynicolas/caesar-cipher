pipeline {
    agent any
    
      tools{
        gradle 'Gradle1'
    }
    
    stages {
        stage('Test') {
            steps {
                echo 'Testing...'
                sh 'gradle test'
           }
        }
        stage('Build') {
            steps {
                echo 'Building..'
                sh 'ls -ali'
                sh './gradlew clean build'      
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                sh 'ps -aux'
                sh './gradlew clean build'
            }
        }
    }
}

